PORT=3000

DATABASE_URL=

JWT_SECRET=
JWT_REFRESH_SECRET=
JWT_EXPIRES_IN=

DEFAULT_PASSWORD=

# user.seeder — email: localKey@domain hoac prefix+localKey@domain (catch-all / Gmail +)
SEED_USER_EMAIL_DOMAIN=example.com
SEED_USER_EMAIL_LOCAL_PREFIX=

CLEANUP_KEEP_ROLES=
# CORS cho Socket.io (US_09); nhieu origin cach nhau bang dau phay. De trong = cho phep moi origin.
# FE ket noi: cung URL server, handshake JWT — vi du socket.io-client: { auth: { token: accessToken } } hoac header Authorization Bearer.
FRONTEND_URL=

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# US_20 — Ingest vi phạm AI từ thiết bị / Python (header x-device-api-key)
# US_10 — thêm POST /api/device/violation (JSON) + Socket ai_violation_alert theo agency_room
MASTER_DEVICE_API_KEY=

MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=