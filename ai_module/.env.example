# Chạy realtime_test: bắt buộc UUID chuyến đang IN_PROGRESS (từ app tài xế / backend)
CURRENT_TRIP_ID=

# Trùng với backend MASTER_DEVICE_API_KEY
MASTER_DEVICE_API_KEY=

# Tuỳ chọn — mặc định trong api_client: http://localhost:3000/api/device/violations
# SMARTDRIVE_DEVICE_INGEST_URL=

# realtime_test.py — CNN mắt: mặc định sigmoid CAO = mắt MỞ. Nếu mở mắt mà báo ngủ/nhắm, thử:
# EYE_MODEL_HIGH_MEANS_OPEN=0
# Ngưỡng P(mở) để coi là MỞ (0.05–0.95), ví dụ khi P(mở) luôn thấp: EYE_OPEN_THRESHOLD=0.35
# EYE_OPEN_THRESHOLD=0.5
