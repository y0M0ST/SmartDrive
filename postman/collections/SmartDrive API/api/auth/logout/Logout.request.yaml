$kind: http-request
name: Logout
url: "{{baseUrl}}/api/auth/logout"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{token}}"
examples: ./.resources/Logout.resources/examples
order: 1000
